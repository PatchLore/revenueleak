{
  "name": "revenueleak",
  "private": true,
  "scripts": {
    "dev": "npm --prefix stripe-leak-scanner run dev",
    "build": "npm --prefix stripe-leak-scanner run build",
    "start": "npm --prefix stripe-leak-scanner run start",
    "vercel-build": "npm --prefix stripe-leak-scanner run build"
  },
  "dependencies": {
    "@react-pdf/renderer": "^4.3.2",
    "@stripe/stripe-js": "^8.8.0",
    "@supabase/auth-helpers-nextjs": "^0.15.0",
    "@supabase/supabase-js": "^2.98.0",
    "bcryptjs": "^3.0.3",
    "next": "^14.2.35",
    "openai": "^6.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^3.7.0",
    "stripe": "^20.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6"
  }
}
